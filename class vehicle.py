# create class
class vehicle:
    # create init method
    def _init_(self, max_speed, mileage):
                # bind the arguments
        self.max_speed = max_speed
        self.mileage = mileage 
    # object creation